
return {
  min_row_count = 1, -- has to be >= 1
  max_row_count = 32,
  default_row_count = 10,

  min_tb_width = 50,
  max_tb_width = 300,
  default_tb_width = 200,

  min_decimal_limit = 0,
  max_decimal_limit = 8, -- 8 seems like it's the highest percission used for positions
  default_decimal_limit = 5,
}
